import { Inter, Righteous, IBM_Plex_Mono } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const righteous = Righteous({ weight: "400", subsets: ["latin"] });

export const ibm = IBM_Plex_Mono({ weight: "400", subsets: ["latin"] });
